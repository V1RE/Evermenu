import { API_TOKEN } from "./preferences";

export const DEFAULT_HEADERS = {
  "Content-Type": "application/json",
  "X-Api-Key": API_TOKEN,
};

export const API_BASE_URL = "https://api.everhour.com";
